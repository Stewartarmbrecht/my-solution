{
  "name": "@my-solution/ui",
  "version": "0.1.1",
  "description": "Contains all shared components across other libraries.",
  "author": "Stewart Armbrecht",
  "license": "ISC",
  "main": "src/index.ts",
  "dependencies": {
    "@tamagui/colors": "*",
    "@tamagui/config": "*",
    "@tamagui/lucide-icons": "*",
    "tamagui": "*"
  },
  "scripts": {
    "lint": "eslint -c ../../.eslintrc.json && prettier --check ./src",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@tamagui/build": "*"
  }
}
